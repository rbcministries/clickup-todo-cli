# Normalize line endings: LF in the repo, auto-converted on checkout.
* text=auto eol=lf

# Treat the JSON spec and generated lock as text.
*.json text eol=lf
*.cs   text eol=lf
*.ps1  text eol=lf

# Binary-ish artifacts (none committed today, but be explicit).
*.png binary
*.nupkg binary
